spring:
  application:
    name: mytodo
  sql:
    init:
      mode: always
mybatis:
  mapper-locations: classpath*:mappers/**/*.xml
  type-aliases-package: com.example.mytodo.web
logging:
  level:
    org.springframework.jdbc: debug
    org.springframework.security: debug
    com.example.mytodo.infra.mybatis.TodoDao: debug
